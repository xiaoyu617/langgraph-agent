An AI Agent is an autonomous system that uses LLMs to reason, make decisions, and take actions. Core capabilities include: Task Planning (breaking down complex goals), Tool Use (calling external functions via Function Calling), Memory Management (short-term conversation history and long-term knowledge), and Self-Reflection (evaluating its own outputs). Agents follow patterns like ReAct (Reasoning + Acting loop) and Plan-Act (plan first, then execute).
